name: My First Workflow
on:
    push:
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v3
            - name: List  & Read files
              run: |
                echo "In this workflow, we are using | to run multiline commands in a single step"
                ls -l
                cat README.md